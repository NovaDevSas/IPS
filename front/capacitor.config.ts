import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.novadevsas.ips',
  appName: 'IPS MedicSalud Sabana',
  webDir: 'build'
};

export default config;
